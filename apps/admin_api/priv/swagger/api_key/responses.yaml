AccessKeyResponse:
  description: Returns a single access key
  content:
    application/vnd.omisego.v1+json:
      schema:
        $ref: 'response_schemas.yaml#/AccessKeyResponseSchema'
AccessKeysResponse:
  description: Returns a list of access keys
  content:
    application/vnd.omisego.v1+json:
      schema:
        $ref: 'response_schemas.yaml#/AccessKeysResponseSchema'

# API KEY RESPONSES
APIKeyResponse:
  description: Returns a single API key
  content:
    application/vnd.omisego.v1+json:
      schema:
        $ref: 'response_schemas.yaml#/APIKeyResponseSchema'
APIKeysResponse:
  description: Returns a list of API keys
  content:
    application/vnd.omisego.v1+json:
      schema:
        $ref: 'response_schemas.yaml#/APIKeysResponseSchema'
